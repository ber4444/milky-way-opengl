// stb_image_impl.cpp — the single TU that emits stb_image's implementation.
//
// stb_image is a "header-only" library whose implementation is gated behind
// STB_IMAGE_IMPLEMENTATION; it must be defined in exactly one .cpp across the
// whole program. Both the iOS and Android builds compile this file.
#define STB_IMAGE_IMPLEMENTATION
#include "third_party/stb_image.h"
